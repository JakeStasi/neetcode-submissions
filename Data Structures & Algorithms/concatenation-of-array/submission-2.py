class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        con = []
        for x in nums:
            con.append(x)
        for x in nums:
            con.append(x)
        return con
        